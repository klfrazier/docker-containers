version: "3.8"

# Paperless-ngx Stack
# Optimized for VM deployment (Proxmox or similar)
# All persistent data stored under ./ for snapshot & backup friendliness

services:

  # -----------------------------
  # Redis (Task Broker / Cache)
  # -----------------------------
  broker:
    image: redis:7
    container_name: paperless-redis
    restart: unless-stopped
    volumes:
      - ./data/redis:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 10s

  # -----------------------------
  # PostgreSQL (Metadata DB)
  # -----------------------------
  db:
    image: postgres:15
    container_name: paperless-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: paperless
      POSTGRES_USER: paperless
      POSTGRES_PASSWORD: paperlesspassword   # CHANGE THIS
    volumes:
      - ./data/postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U paperless"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 20s

  # -----------------------------
  # Gotenberg (PDF Conversion)
  # -----------------------------
  gotenberg:
    image: gotenberg/gotenberg:7
    container_name: paperless-gotenberg
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 20s

  # -----------------------------
  # Apache Tika (Text Extraction)
  # -----------------------------
  tika:
    image: apache/tika:latest
    container_name: paperless-tika
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:9998"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 20s

  # -----------------------------
  # Paperless Web + Worker
  # -----------------------------
  webserver:
    image: ghcr.io/paperless-ngx/paperless-ngx:latest
    container_name: paperless-web
    restart: unless-stopped

    # Ensure services are healthy before startup
    depends_on:
      db:
        condition: service_healthy
      broker:
        condition: service_healthy
      gotenberg:
        condition: service_healthy
      tika:
        condition: service_healthy

    ports:
      - "8000:8000"

    environment:
      # Core Services
      PAPERLESS_REDIS: redis://broker:6379
      PAPERLESS_DBHOST: db

      # OCR + PDF
      PAPERLESS_TIKA_ENABLED: 1
      PAPERLESS_TIKA_GOTENBERG_ENDPOINT: http://gotenberg:3000
      PAPERLESS_TIKA_ENDPOINT: http://tika:9998

      # System
      PAPERLESS_TIME_ZONE: America/New_York
      USERMAP_UID: 1000
      USERMAP_GID: 1000

      # Recommended for production
      PAPERLESS_CONSUMER_POLLING: 10
      PAPERLESS_TASK_WORKERS: 2

    volumes:
      - ./media:/usr/src/paperless/media
      - ./consume:/usr/src/paperless/consume
      - ./export:/usr/src/paperless/export
      - ./data:/usr/src/paperless/data

    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8000"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 40s